using System;

namespace NumericalSequence
{
    class Program
    {
        static void Main(string[] args)
        {
            int n = int.Parse(args[0]);
            int m = (int)Math.Sqrt(int.Parse(args[1]));
            //int[] a = new int[n];
            if a*a
            int a = m;
            for (int i = 0; i < n; i++)
            {
                a++;
                //= a[i-1]+1;
                Console.Write($"{a} ");
            }
        }

        public class NumericalSeries
        {
            private int n;
            private i
        }
    }
}
